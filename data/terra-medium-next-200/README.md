# Terra Medium batch output

Terra Medium writes `batch-1.json` through `batch-8.json` here. Each file must follow `docs/TERRA_MEDIUM_NEXT_200.md` and pass `npm run validate:terra-medium-next-200 -- --batch=N`.

Do not add a placeholder decision file. Missing batches deliberately block composition and Sol review.
